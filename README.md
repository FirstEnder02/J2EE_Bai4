The code will run on port 8080, the follow commit for Git will be A6
Currently bugged with:
- Can't apple Bootstrap, the "Create New Product" need a button.
- /products/create can't be access. (Mispelled somewhere from product/create instead of products/create, too)
- Need to create local image so it can load image in website.
- Need some basic category.
- Can't create product.
- Need delete function
- Need a header and footer that on have black color on it.